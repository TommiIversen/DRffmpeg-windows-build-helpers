diff --git a/fftools/ffmpeg.c b/fftools/ffmpeg.c
index b394243f59..3e3bd0eaee 100644
--- a/fftools/ffmpeg.c
+++ b/fftools/ffmpeg.c
@@ -86,9 +86,21 @@
 #include "ffmpeg_utils.h"
 #include "graph/graphprint.h"
 
-const char program_name[] = "ffmpeg";
+const char program_name[] = "ffmpeg 2mmilab " __DATE__ " " __TIME__;
 const int program_birth_year = 2000;
 
+static uint64_t total_size_bytes = 0;
+int slow_delay = 0;
+int slow = 0;
+
+void ff_add_input_bytes(int size)
+{
+    if (size > 0)
+    {
+        total_size_bytes += size;
+    }
+}
+
 FILE *vstats_file;
 
 typedef struct BenchmarkTimeStamps {
@@ -694,6 +706,9 @@ static void print_report(int is_last_report, int64_t timer_start, int64_t cur_ti
         av_bprintf(&buf_script, "speed=%4.3gx\n", speed);
     }
 
+    av_bprintf(&buf, " cursor=%" PRId64, total_size_bytes);
+    av_bprintf(&buf, " slow=%d", slow);
+
     secs = (int)t;
     ms = (int)((t - secs) * 1000);
     mins = secs / 60;
@@ -878,6 +893,17 @@ static int check_keyboard_interaction(int64_t cur_time)
                         "s      Show QP histogram\n"
         );
     }
+
+    /*slow down sleep */
+    if (key == '1'){
+        slow_delay = 100000;
+        slow = 1;
+    }
+    if (key == '0') {
+        slow_delay = 0;
+        slow = 0;
+    }
+
     return 0;
 }
 
diff --git a/fftools/ffmpeg.h b/fftools/ffmpeg.h
index b4d3a2c2ac..7c074b4d03 100644
--- a/fftools/ffmpeg.h
+++ b/fftools/ffmpeg.h
@@ -986,4 +986,9 @@ int view_specifier_parse(const char **pspec, ViewSpecifier *vs);
 int muxer_thread(void *arg);
 int encoder_thread(void *arg);
 
-#endif /* FFTOOLS_FFMPEG_H */
+
+extern int slow_delay;
+extern int slow;
+void ff_add_input_bytes(int size);
+
+#endif /* FFTOOLS_FFMPEG_H */
\ No newline at end of file
diff --git a/fftools/ffmpeg_dec.c b/fftools/ffmpeg_dec.c
index 5020684a28..1cb6669e75 100644
--- a/fftools/ffmpeg_dec.c
+++ b/fftools/ffmpeg_dec.c
@@ -715,6 +715,11 @@ static int packet_decode(DecoderPriv *dp, AVPacket *pkt, AVFrame *frame)
     }
 
     if (pkt) {
+
+        if (pkt->size > 0) { // Ensure we only add positive sizes
+            ff_add_input_bytes(pkt->size); // Call the function from ffmpeg.c
+        }
+
         FrameData *fd = packet_data(pkt);
         if (!fd)
             return AVERROR(ENOMEM);
diff --git a/fftools/ffmpeg_demux.c b/fftools/ffmpeg_demux.c
index 7c708ff0f3..8867b0ddae 100644
--- a/fftools/ffmpeg_demux.c
+++ b/fftools/ffmpeg_demux.c
@@ -468,6 +468,11 @@ static int input_packet_process(Demuxer *d, AVPacket *pkt, unsigned *send_flags)
     FrameData *fd;
     int ret = 0;
 
+    /*slow down 1 = slow 0 = full speed*/
+    if (slow_delay > 0) {
+        av_usleep(slow_delay);
+    }
+
     fd = packet_data(pkt);
     if (!fd)
         return AVERROR(ENOMEM);
